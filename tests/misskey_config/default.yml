# for misskey server. see below:
# https://github.com/misskey-dev/misskey/blob/develop/.config/example.yml

url: http://misskey-web/

port: 3000

db:
  host: db
  port: 5432
  db: misskey
  user: example-misskey-user
  pass: example-misskey-pass

redis:
  host: redis
  port: 6379

id: 'aid'
disableHsts: true
